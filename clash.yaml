[Config]
; 基础设置
mixed-port: 7890
allow-lan: true
mode: rule
log-level: info
external-controller: 127.0.0.1:9090

[Proxy]
; 这里留空，Sub-Check 会自动填充测速后的节点

[Proxy Group]
; 只保留一个最简单的分组
- name: "🚀 节点列表"
  type: select
  proxies:
    - DIRECT

[Rule]
; 只有一条兜底规则
- MATCH,🚀 节点列表
